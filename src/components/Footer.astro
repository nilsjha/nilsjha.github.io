---
interface Props {
  home?: boolean;
  separator?: boolean;
}

const { name: name = false, separator = false } = Astro.props;
const today = new Date();
---

<footer
  class="
  text-base
  md:text-base
  shrink-0
  mx-auto
  uppercase
  tracking-widest
  flex flex-row gap-4
  px-8
  py-8"
>
  <span class="flex flex-row gap-1">
    {today.getFullYear()}
  </span>
  <span class={name ? "" : "hidden"}>
    <a class="_hotlink" href="/">main menu</a></span
  >
  <span class={name ? "block" : "hidden"}>
    <span class="">■</span>
    <span class="flex flex-col md:flex-row md:gap-4">
     Nils Jarle Haugen<span class="text-slate-400 hover:animate-degauss"
      ><a
        href="https://www.fjordnorway.com/en/destinations/alesund--geiranger"
        class="hover:animate-degauss"
        target="_blank">ålesund</a
      ></span
    > norway
    </span>
  </span>
  <div class="hidden md:block">
    <span class={separator ? "" : "hidden"}></span>
    <slot />
  </div>
</footer>
